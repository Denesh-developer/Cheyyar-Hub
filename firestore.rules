
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /emailOtpChallenges/{email} {
      allow read, write: if false;
    }

    // Keep your existing rules for all other collections.
  }
}
